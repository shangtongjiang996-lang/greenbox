[build]
builder = "NIXPACKS"

[build.nixpacks]
plan = '''
[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "npm start"
'''